---
# WARNING: Replace placeholder values before applying to a real cluster.
# For production, use sealed-secrets, external-secrets, or Vault instead of plain secrets.
apiVersion: v1
kind: Secret
metadata:
  name: secrets
  labels:
    app.kubernetes.io/name: knowledge-graph-api
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  NEO4J_AUTH: "neo4j/changeme"
  NEO4J_USER: "neo4j"
  NEO4J_PASSWORD: "changeme"
  POSTGRES_USER: "kgadmin"
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql+asyncpg://kgadmin:changeme@kga-postgres:5432/knowledge_graph"
  OPENAI_API_KEY: "sk-your-key-here"
